<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Récupération de mot de passe</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .password-container {
            max-width: 500px;
            margin: 100px auto;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: white;
        }
        .password-header {
            text-align: center;
            margin-bottom: 30px;
            color: #333;
        }
        .btn-submit {
            width: 100%;
            padding: 10px;
            background-color: #0d6efd;
            border: none;
            transition: background-color 0.3s;
        }
        .btn-submit:hover {
            background-color: #0b5ed7;
        }
        .form-control {
            padding: 12px;
            margin-bottom: 20px;
        }
        .message {
            margin-top: 20px;
            text-align: center;
            padding: 10px;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="password-container">
            <h2 class="password-header">Mot de passe oublié</h2>
            <form action="/forgot-password" method="post">
                <div class="mb-3">
                    <input type="email" class="form-control" name="email" placeholder="Votre email" required>
                </div>
                <button type="submit" class="btn btn-primary btn-submit">Envoyer le lien</button>
            </form>
            
            <% if (message) { %>
                <div class="message alert <%= message.includes('Erreur') ? 'alert-danger' : 'alert-success' %>">
                    <%= message %>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Bootstrap 5 JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>